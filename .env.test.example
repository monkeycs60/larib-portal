# Test Environment Variables
# Copy this file to .env.test and fill in your test database credentials

# Test Database (Neon DB - separate from dev and prod)
# Example: postgresql://neondb_owner:npg_xxx@ep-xxx.eu-central-1.aws.neon.tech/testdb?sslmode=require&channel_binding=require
DATABASE_URL="postgresql://neondb_owner:npg_5BW4rDpyNqRC@ep-old-hat-a2cwckco-pooler.eu-central-1.aws.neon.tech/testdb?sslmode=require&channel_binding=require"

# Better Auth (use same secret as dev or generate new one)
BETTER_AUTH_SECRET="your-test-secret-key-change-this"
BETTER_AUTH_URL="http://localhost:3000"

# Google OAuth (optional for tests, can use dev credentials)
GOOGLE_CLIENT_ID="your-google-client-id-optional"
GOOGLE_CLIENT_SECRET="your-google-client-secret-optional"

# NOTE: Test users are created by the seed script (prisma/seed.test.ts)
# Admin credentials: test-admin@larib-portal.test / ristifou
# User credentials:  test-user@larib-portal.test / ristifou
